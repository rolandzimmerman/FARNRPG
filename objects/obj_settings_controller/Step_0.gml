audio_group_set_gain(audio_group_sfx, global.sfx_volume, 0);
audio_group_set_gain(audio_group_music, global.music_volume, 0);

/*if (keyboard_check_pressed(ord("Z"))) {
    audio_play_sound(snd_attack, 1, false); // test sound
}
